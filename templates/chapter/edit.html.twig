{% extends 'base.html.twig' %}

{% block title %}Modifier {{ chapter.title }}{% endblock %}

{% block body %}
<div class="relative min-h-screen overflow-hidden bg-slate-50/50">
    <!-- Fond décoratif -->
    <div class="absolute inset-0 z-0">
        <div class="absolute top-0 -right-96 w-[800px] h-[800px] bg-gradient-to-bl from-purple-50 to-indigo-100/50 rounded-full blur-3xl"></div>
        <div class="absolute -bottom-96 -left-96 w-[800px] h-[800px] bg-gradient-to-tr from-blue-50 to-cyan-100/50 rounded-full blur-3xl"></div>
    </div>

    <!-- Contenu principal -->
    <div class="relative z-10">
        <div class="max-w-3xl mx-auto px-4 sm:px-6 lg:px-8 py-12">
            <!-- En-tête -->
            <div class="mb-8">
                <nav class="flex mb-8" aria-label="Breadcrumb">
                    <ol class="inline-flex items-center space-x-1 md:space-x-3">
                        <li class="inline-flex items-center">
                            <a href="{{ path('app_subject_index') }}" class="inline-flex items-center text-sm font-medium text-slate-700 hover:text-slate-900">
                                <i class="fas fa-book mr-2"></i>
                                Matières
                            </a>
                        </li>
                        <li>
                            <div class="flex items-center">
                                <i class="fas fa-chevron-right text-slate-400 mx-2 text-sm"></i>
                                <a href="{{ path('app_subject_show', {'id': chapter.subject.id}) }}" class="text-sm font-medium text-slate-700 hover:text-slate-900">{{ chapter.subject.name }}</a>
                            </div>
                        </li>
                        <li>
                            <div class="flex items-center">
                                <i class="fas fa-chevron-right text-slate-400 mx-2 text-sm"></i>
                                <a href="{{ path('app_chapter_show', {'id': chapter.id}) }}" class="text-sm font-medium text-slate-700 hover:text-slate-900">{{ chapter.title }}</a>
                            </div>
                        </li>
                        <li>
                            <div class="flex items-center">
                                <i class="fas fa-chevron-right text-slate-400 mx-2 text-sm"></i>
                                <span class="text-sm font-medium text-slate-500">Modifier</span>
                            </div>
                        </li>
                    </ol>
                </nav>

                <h2 class="text-3xl font-bold leading-7 text-slate-900 sm:truncate sm:tracking-tight mb-1">
                    Modifier {{ chapter.title }}
                </h2>
                <p class="text-sm text-slate-500">Dans {{ chapter.subject.name }}</p>
            </div>

            <!-- Formulaire -->
            <div class="bg-white shadow-sm ring-1 ring-slate-700/5 rounded-xl">
                <div class="px-4 py-5 sm:p-6">
                    {{ form_start(form, {'attr': {'class': 'space-y-6'}}) }}
                        <div>
                            {{ form_label(form.title, 'Titre du chapitre', {'label_attr': {'class': 'block text-sm font-medium text-slate-700'}}) }}
                            <div class="mt-1">
                                {{ form_widget(form.title, {'attr': {'class': 'block w-full rounded-md border-slate-300 shadow-sm focus:border-indigo-500 focus:ring-indigo-500 sm:text-sm', 'placeholder': 'Ex: Introduction à la programmation'}}) }}
                            </div>
                            {{ form_errors(form.title, {'attr': {'class': 'text-red-500 text-sm mt-1'}}) }}
                        </div>

                        <div>
                            {{ form_label(form.content, 'Contenu', {'label_attr': {'class': 'block text-sm font-medium text-slate-700'}}) }}
                            <div class="mt-1">
                                {{ form_widget(form.content, {'attr': {'class': 'block w-full rounded-md border-slate-300 shadow-sm focus:border-indigo-500 focus:ring-indigo-500 sm:text-sm', 'rows': '8', 'placeholder': 'Contenu détaillé du chapitre...'}}) }}
                            </div>
                            {{ form_errors(form.content, {'attr': {'class': 'text-red-500 text-sm mt-1'}}) }}
                        </div>

                        <div>
                            {{ form_label(form.subject, 'Matière', {'label_attr': {'class': 'block text-sm font-medium text-slate-700'}}) }}
                            <div class="mt-1">
                                {{ form_widget(form.subject, {'attr': {'class': 'block w-full rounded-md border-slate-300 shadow-sm focus:border-indigo-500 focus:ring-indigo-500 sm:text-sm'}}) }}
                            </div>
                            {{ form_errors(form.subject, {'attr': {'class': 'text-red-500 text-sm mt-1'}}) }}
                        </div>

                        <div class="flex justify-end space-x-3">
                            <a href="{{ path('app_chapter_show', {'id': chapter.id}) }}" class="inline-flex items-center px-4 py-2 border border-slate-300 shadow-sm text-sm font-medium rounded-md text-slate-700 bg-white hover:bg-slate-50 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-indigo-500">
                                <i class="fas fa-times mr-2"></i>
                                Annuler
                            </a>
                            <button type="submit" class="inline-flex items-center px-4 py-2 border border-transparent text-sm font-medium rounded-md text-white bg-indigo-600 hover:bg-indigo-700 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-indigo-500">
                                <i class="fas fa-save mr-2"></i>
                                Enregistrer
                            </button>
                        </div>
                    {{ form_end(form) }}
                </div>
            </div>
        </div>
    </div>
</div>
{% endblock %}
